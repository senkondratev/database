<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" type="text/css" href="/static/myStyle.css">
    <title>reservationInsertPage</title>
    <meta http-equiv="content-type" content="text/html; charset=UTF-8">
    <script src="https://code.jquery.com/jquery-2.2.4.min.js" integrity="sha256-BbhdlvQf/xTY9gja0Dq3HiwQF8LaCRTXxZKRutelT44=" crossorigin="anonymous"></script>
    <script type="text/javascript" src="/static/insert/reservation/reservationDelete.js"></script>
</head>
<body>
<div>
    <form method="get" action="/insert">
        <button type="submit">Back</button>
    </form>
</div>
<div>
    {{status}}
</div>
    <div>
        <form action="/insert/reservation" method="post">
            <select name="buildingId" required>
                <option value="" disabled selected>Выберите здание по ID</option>
                {{#buildings}}
                    <option value={{buildingId}}>{{buildingId}}</option>
                {{/buildings}}
            </select>
            Начало брони:<input type="date" name="startDate" required>
             Окончание:<input type="date" name="endDate" required>
            <button type="submit">ok</button>
        </form>
    </div>
<div id="table">
    <table>
        <caption>Брони</caption>
        <tr>
            <th>ID брони</th>
            <th>ID здания</th>
            <th>Число звезд здания</th>
            <th>Дата начала</th>
            <th>Дата окончания</th>
            <th>Удалить</th>
        </tr>
        {{#reservations}}
            <tr>
                <td>{{reservationId}}</td>
                <td>{{buildingId}}</td>
                <td>{{buildingLevel}}</td>
                <td>{{startDate}}</td>
                <td>{{endDate}}</td>
                <td>
                    <button type="submit" id="deleteReservation" value={{reservationId}} >Delete</button>
                </td>
            </tr>
        {{/reservations}}
    </table>
</div>
</body>
</html>