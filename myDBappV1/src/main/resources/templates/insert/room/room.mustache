<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" type="text/css" href="/static/myStyle.css">
    <title>roomInsertPage</title>
    <meta http-equiv="content-type" content="text/html; charset=UTF-8">
    <script src="https://code.jquery.com/jquery-2.2.4.min.js" integrity="sha256-BbhdlvQf/xTY9gja0Dq3HiwQF8LaCRTXxZKRutelT44=" crossorigin="anonymous"></script>
    <script type="text/javascript" src="/static/insert/room/roomDelete.js"></script>
    <script type="text/javascript" src="/static/insert/room/roomValidate.js"></script>
</head>
<body>
<div>
    <form method="get" action="/insert">
        <button type="submit">Back</button>
    </form>
</div>
    <div>
        <form method="post" action="/insert/room">
            <select name="buildingId" id="buildingSelect" required>
                <option disabled selected value=""> Выберите здание по ID</option>
                {{#buildings}}
                    <option value={{buildingId}}>{{buildingId}}</option>
                {{/buildings}}
            </select>
            Выгода в день: <input name="profit" type="number" min="1" max="100000" placeholder="profit" required>
            <div id="floorSelect">
            Этаж: <input name="floor" type="number" min="1" max="5" width="200" placeholder="floor" disabled>
            </div>
            Вместимость: <input name="capacity" type="number" min="1" max="4" width="200" placeholder="capacity" required>
            <button type="submit">ok</button>
        </form>
    </div>
<br>
А еще вы можете отсортировать значения в таблице по выбранному атрибуту
<form method="get" action="/insert/roomSort">
    <select name="attribute" required>
        <option selected disabled value="">Выберите атрибут</option>
        <option value="roomId">ID комнаты</option>
        <option value="Building_BuildingId">ID здания</option>
        <option value="roomProfit">Прибыль в день</option>
        <option value="roomFloor">Этаж</option>
        <option value="roomCapacity">Вместимость номера</option>
    </select>
    <select name="sort" required>
        <option selected disabled>Выберите тип сортировки</option>
        <option value="0">ASC</option>
        <option value="1">DESC</option>
    </select>
    <button type="submit">Ok</button>
</form>
<br>
<div id="table">
    <table>
        <caption>Комнаты</caption>
        <tr>
            <th>ID комнаты</th>
            <th>ID здания</th>
            <th>Прибыль в день</th>
            <th>Этаж</th>
            <th>Вместимость</th>
            <th>Удалить</th>
        </tr>
        {{#rooms}}
            <tr>
                <td>{{roomId}}</td>
                <td>{{buildingId}}</td>
                <td>{{roomProfit}}</td>
                <td>{{roomFloor}}</td>
                <td>{{roomCapacity}}</td>
                <td>
                    <button type="submit" id="deleteRoom" value={{roomId}} >Delete</button>
                </td>
            </tr>
        {{/rooms}}
    </table>
</div>
</body>
</html>