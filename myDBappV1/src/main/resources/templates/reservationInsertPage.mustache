<!DOCTYPE html>
<html>
<head>
    <title>reservationInsertPage</title>
    <meta http-equiv="content-type" content="text/html; charset=UTF-8">
</head>
<body>
    <div>
        <form action="/insert/reservation" method="post">
            <select name="buildingId">
                {{#buildings}}
                    <option value={{buildingId}}>it's building {{buildingId}}</option>
                {{/buildings}}
            </select>
            <input type="date" name="startDate">
            <input type="date" name="endDate">
            <button type="submit">ok</button>
        </form>
    </div>
    <div>
        Our Reservations
    </div>
    {{#reservations}}
        {{reservationId}}
        {{buildingId}}
        {{buildingLevel}}it's level
        {{startDate}}
        {{endDate}}
        <br>
    {{/reservations}}
</body>
</html>