<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" type="text/css" href="/static/myStyle.css">
    <title>roomInsertPage</title>
    <meta http-equiv="content-type" content="text/html; charset=UTF-8">
    <script src="https://code.jquery.com/jquery-2.2.4.min.js" integrity="sha256-BbhdlvQf/xTY9gja0Dq3HiwQF8LaCRTXxZKRutelT44=" crossorigin="anonymous"></script>
    <script type="text/javascript" src="/static/insert/room/roomDelete.js"></script>
    <script type="text/javascript" src="/static/insert/room/roomValidate.js"></script>
</head>
<body>
<div>
    <form method="get" action="/insert">
        <button type="submit">Back</button>
    </form>
</div>
    <div>
        <form method="post" action="/insert/room">
            <select name="buildingId" id="buildingSelect">
                <option disabled selected> Выберите здание</option>
                {{#buildings}}
                    <option value={{buildingId}}>{{buildingId}} id здания</option>
                {{/buildings}}
            </select>
            <input name="profit" type="number" max="100000" placeholder="profit">
            <div id="floorSelect">
            <input name="floor" type="number" min="1" max="5" width="200" placeholder="floor">
            </div>
            <input name="capacity" type="number" min="1" max="400" width="200" placeholder="capacity">
            <button type="submit">ok</button>
        </form>
    </div>
<div id="table">
    <table>
        <caption>Комнаты</caption>
        <tr>
            <th>ID комнаты</th>
            <th>ID здания</th>
            <th>Прибыль в день</th>
            <th>Этаж</th>
            <th>Вместимость</th>
            <th>Удалить</th>
        </tr>
        {{#rooms}}
            <tr>
                <td>{{roomId}}</td>
                <td>{{buildingId}}</td>
                <td>{{roomProfit}}</td>
                <td>{{roomFloor}}</td>
                <td>{{roomCapacity}}</td>
                <td>
                    <button type="submit" id="deleteRoom" value={{roomId}} >Delete</button>
                </td>
            </tr>
        {{/rooms}}
    </table>
</div>
</body>
</html>